#include "monitor.h"

/* Stage 1 commit 1 stub. The real lexer + parser + evaluator lands in
 * the next commit. */
word_t expr(const char *s, bool *success) {
    (void)s;
    *success = false;
    return 0;
}
